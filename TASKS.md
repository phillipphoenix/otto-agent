# Tasks: Add version flag to CLI (#17)

- [ ] Add `--version` / `-v` option to `parseArgs` and handle it (print version, exit 0) — file(s): `src/cli.ts`
- [ ] Update `showHelp()` to document the `--version` flag — file(s): `src/cli.ts`
