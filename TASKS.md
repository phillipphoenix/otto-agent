# Tasks: Update install scripts to include an introduction/help section (#15)

- [ ] Add Otto intro block to `install.sh` after the installed-path echo — file(s): `install.sh`
- [ ] Add Otto intro block to `install.ps1` after the installed-path Write-Host — file(s): `install.ps1`
