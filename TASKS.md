# Tasks: Setup testing with bun test runner (#6)

- [ ] Add `"test": "bun test"` to scripts in `package.json` — file(s): `package.json`
- [ ] Create test file for `parseFrontmatter` covering valid frontmatter, missing fields, and no frontmatter — file(s): `src/primitives/frontmatter.test.ts`
