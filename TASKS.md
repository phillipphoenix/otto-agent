# Tasks: When using the issue-orchestrator the final result text is injected in before the child iteration reports (#23)

- [x] Move nested iterations block above result summary block, and add marginTop={1} to result summary — file(s): `src/ui/IterationView.tsx`
